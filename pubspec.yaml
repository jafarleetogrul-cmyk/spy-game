name: spy_game
description: "Spy Game – Spyfall style crossplatform multiplayer game"
publish_to: "none"
version: 2.0.0+1

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Networking
  http: ^1.2.0

  # QR
  qr_flutter: ^4.1.0
  mobile_scanner: ^4.0.0

  # State
  provider: ^6.1.2

  # UI
  google_fonts: ^6.2.1
  flutter_animate: ^4.5.0

  # Storage (leaderboard + history)
  shared_preferences: ^2.2.3

  # Sound
  audioplayers: ^5.2.1

  # Vibration
  vibration: ^1.8.4

  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true
  assets:
    - assets/sounds/
